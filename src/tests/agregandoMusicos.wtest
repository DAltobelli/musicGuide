import pdpalooza.*
import Musico.*

describe "tests para agregar musicos"{
	const messi = new VocalistaPopular(100,"no borres el pizarron")
	
	test "messi no puede entrar en la pdppalooza"{
		pdpalooza.agregarCantante(messi)
		assert.notThat(pdpalooza.tocaMusico(messi))
	}
}